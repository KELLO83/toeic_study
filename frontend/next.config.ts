import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler: true, // Disabling this as it might be causing Turbopack panics on Windows
};

export default nextConfig;
